{
  "name": "nextn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd workspace && npm run dev",
    "genkit:dev": "cd workspace && npm run genkit:dev",
    "genkit:watch": "cd workspace && npm run genkit:watch",
    "build": "cd workspace && npm run build",
    "start": "cd workspace && npm run start",
    "lint": "cd workspace && npm run lint",
    "typecheck": "cd workspace && npm run typecheck"
  },
  "dependencies": {
    "genkit": "^1.13.0",
    "tsx": "^4.16.2"
  },
  "devDependencies": {
    "genkit-cli": "^1.13.0"
  }
}
