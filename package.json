{
  "name": "nextn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w workspace",
    "build": "npm run build -w workspace",
    "start": "npm run start -w workspace",
    "lint": "npm run lint -w workspace",
    "typecheck": "npm run typecheck -w workspace",
    "genkit:dev": "npm run genkit:dev -w workspace",
    "genkit:watch": "npm run genkit:watch -w workspace"
  },
  "workspaces": [
    "workspace"
  ]
}
