
{
  "name": "nextn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev -w workspace --port 9002 --hostname 0.0.0.0",
    "genkit:dev": "genkit start -- tsx src/ai/dev.ts",
    "genkit:watch": "genkit start -- tsx --watch src/ai/dev.ts",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit"
  },
  "workspaces": [
    "workspace"
  ],
  "dependencies": {
    "genkit": "^1.13.0",
    "tsx": "^4.16.2"
  },
  "devDependencies": {
    "genkit-cli": "^1.13.0",
    "typescript": "^5"
  }
}
